# skills

Public, consume-only versions of selected OutSystems agent skills.

Each subdirectory here is a **derived export** of a skill whose canonical source
lives in `portable-agent-skills`. Contents are generated by a scrub-and-export
step and must not be hand-edited — upstream the change in `portable-agent-skills`
and re-export instead.

No skills have been exported yet.
