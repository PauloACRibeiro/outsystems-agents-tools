# Mentor Operations Registry

Rule (adapted from OutSystems/o11toodc-conversions-marketplace, mirroring
`odc-error-registry.md`): this registry is the canonical INDEX of Mentor and
OutSystems-MCP operation cadence — one row per operation (or tier-grouped
operation family), giving its polling tier, poll cadence, typical duration,
cancel guidance, and a session-economics note. Skills resolve an operation's
cadence here first. `outsystems-mentor-polling-behavior` is the owning skill
for tier definitions, cadence values, and drain/pause mechanics and keeps the
full treatment; a skill that needs more than the row provides follows that
skill's `SKILL.md`. Adding a competing cadence explanation to a file that is
not the owner, or letting a row drift from the owner's text, is a lint
finding.

| Operation | Tier | Poll cadence | Typical duration | Cancel guidance | Session-economics note |
|---|---|---|---|---|---|
| Tier 1 read/query tools: `app_list`, `app_info`, `app_refs`, `env_list`, `env_apps`, `context_screens`, `context_entities`, `context_actions`, `context_roles`, `context_structures`, `context_themes`, `context_search` | Tier 1 (sync) | No polling — call once, use the result immediately; no sleep, no cursor, no retry loop. | Synchronous / immediate. | Not applicable — no async run exists to cancel. | Zero poll cost by design; never add a sleep or poll loop before using these tools' results, and never apply Tier 3 sleep discipline to them. |
| Tier 2 async tools: `publish_start` → `publish_status`; `deploy_start` → `deploy_status`; `extlib_upload` → `extlib_status`; `test_setup_start` → `test_setup_status` | Tier 2 (fast async) | ~15s between status checks (upstream 0.13.1 accepts 5–15s; this catalog defaults to the token-lean 15s end of that range); never poll faster than 5s. | Typically 30–90s. | None of this registry's sources name a cancel tool for these operations — no cancel guidance is documented for Tier 2 runs. | Intermediate poll responses are compact (a status string and a key), so frequent polling costs little in tokens; still, don't apply Tier 1's no-sleep behavior here (hammering `publish_status` etc. without sleep burns tokens on frequent-but-compact responses), and don't apply Tier 3's 30s pause either — Tier 2 completes faster and a 30s wait is needlessly slow. |
| Mentor authoring run: `mentor_start` → `mentor_get_run` (the only Tier 3 tool) | Tier 3 (long async / verbose) | Drain-then-pause: poll immediately after `mentor_start`, and again immediately while `nextCursor` advances (events are cursor-paged and batched — draining is correctness, not token waste). Pause ~30s (`tier3CadenceSecs`) only once a poll returns no new events and `status` is non-terminal. Always pass the previous response's `nextCursor`; never omit it once non-null. Stop at `succeeded` / `failed` / `cancelled`. | `outsystems-mentor-polling-behavior`: runs typically take 2–10 minutes (~8–15 polls at the 30s drained cadence). OMI field measurement (`execution-gates.md`): healthy small turns reach terminal in 1–5 min; heavy structural turns legitimately run 8–12 min with ~7 quiet minutes being normal — judge "stuck" against the turn's size, not a flat timer. | `mcp__outsystems__mentor_cancel(runId)`; fire-and-forget — poll once after to confirm `cancelled`, then stop (`outsystems-mentor-copilot`). Give a heavy turn ~12–14 min before considering cancel, and only cancel if you also intend to split the work smaller — a cancel that re-runs the same oversized prompt buys nothing. Cancel costs >3 min to settle, a cancelled turn commits nothing (failed/cancelled turns never advance OML), and cancelling an already-succeeded run is a no-op you will misread as a wedge — re-poll the `runId` before concluding anything (`execution-gates.md`). Hang tell: a `nextCursor` unchanged for ~7–10 minutes is the cursor-side signature of a wedge; one `details: true` poll confirms before applying the cancel calibration above. | ~8–15 polls at the drained 30s cadence vs ~240–600 polls at the server's 500ms `pollAfterMs` hint — 30–40× more tokens (`outsystems-mentor-polling-behavior`). Real field runs (`outsystems-mentor-copilot`, `morpheus-qa` tenant): 3 polls / ~30K tokens for a warm-cache `doc-gap-fill`; 11 polls / ~300K tokens for a cold-cache `demo-readiness` run, dominated by one 30–500KB `tool_end` OML-scan event that lands once and is re-read as cache on every later turn. A poll loop past ~20 iterations with no terminal state signals an over-broad prompt — cancel and narrow scope rather than continuing to poll. |
